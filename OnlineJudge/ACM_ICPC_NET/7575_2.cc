#include <bits/stdc++.h>
using namespace std;
#define PI 2*acos(0.0)
#define INF 1e8
#define EPSILON 1e-8
#ifdef DEBUG
#define DPRINTF(x) printf x
#else
#define DPRINTF(x) ;
#endif

typedef pair<int, int> pii;
typedef pair<int, pair<int, int> > piii;
typedef vector<int> vi;
typedef vector<pii> vpii;
typedef vector<bool> vb;
typedef vector<string> vs;

int main () {
	scanf("%d%d", &n, &k);
	for (int i=0; i<n; ++i) {
		scanf("%d", &s[i]);
		for (int j=0; j<s[i]; ++j)
			scanf("%d", &a[i][j]);
	}

	int m=0;
	for (int i=0; i<s[0]; ++i) {
	
	for (int i=1; i<n; ++i) {
		for (int start=0; start<size[i]-k; ++start) {
			


	return 0;
}


